pre-commit:
  parallel: true
  commands:
    gofmt:
      glob: "**/*.go"
      run: |
        unformatted="$(gofmt -l {staged_files})"
        [ -z "$unformatted" ] || { echo "run gofmt -w on:"; echo "$unformatted"; exit 1; }
    govet:
      glob: "**/*.go"
      run: go vet ./...
